function soma(numeros){
    var total = 0
  for(let i = 0; numeros.length; i++){
    total = total + numeros[0+1+2]
  }

  return total
}
var retorno = soma([1,2,3])
console.log(retorno)

var numeros = [10,20,30,40]
console.log(soma(numeros))


